

<script>
   

</script>



   @(Html.Kendo().TabStrip()
       .Name("tabUserRol")
       .Items(tabstrip =>
       {
           tabstrip.Add().Text("Por Rol")
           //.ImageUrl(Url.Content("~/shared/icons/sports/baseball.png"))
           //.Content(@Url.Action("CustomersOfCustomer", "CustomerOfCustomer"));
           .LoadContentFrom("PorRol", "UserRol");

           tabstrip.Add().Text("Por Usuario")
           //.ImageUrl(Url.Content("~/shared/icons/sports/baseball.png"))
           .LoadContentFrom(@Url.Action("PorUsuario", "UserRol"));

          

       })
       .SelectedIndex(0)
    )

@(Html.Kendo().Window().Name("wComprobanteRetencion").Draggable()
      .Title("Comprobante de Retención")
      .Draggable(true)
      .Resizable()
      .Visible(false)
      .Width(900)
      .Height(600)
      .LoadContentFrom("pvwComprobante", "ComprobanteRetencion"))





<script>


    function onsave(e) {
      //  console.log(e.model);
        var rolid = $("#hrolid").val();
         var userid = $("#huserid").val();

        e.model.set("UserId", userid);
          e.model.set("RoleId", rolid);
    }


    function EditHandler(e) {
    //  var gridB = $("#gridUsersRoles").getKendoGrid(),
         //gridB.dataSource.read({Id: model.Id});
        var model = e.model;
      //  console.log(model);   
    }

        function rolf(e) {
           // console.log(e);
            var dataItem = e.dataItem;
            $("#hrolid").val(dataItem.Id);
            $("#hrolidname").val(dataItem.Name);
      
    }

    function userf(e) {
        //  console.log(e);
        var dataItem = e.dataItem;
        $("#huserid").val(dataItem.Id);
        $("#huseridname").val(dataItem.UserName);

    }
</script>