@model DateTime?

@(Html.Kendo().DatePickerFor(m => m)
         .Format("dd/MM/yyyy")
         .ParseFormats(new string[] { "dd/MM/yyyy", "dd-MM-yyyy" }).HtmlAttributes(new { required = "required", data_required_msg = "El campo fecha es requerido" })
)