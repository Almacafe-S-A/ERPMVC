@model ERPMVC.Models.CustomerType

@(Html.Kendo().DropDownListFor(m => m)
            .AutoBind(true)
            .DataValueField("CustomerTypeId")
            .DataTextField("CustomerTypeName")
            .Events(e =>
            {
                e.Change("changesetearvalues");
            })
            .BindTo((System.Collections.IEnumerable)ViewData["CustomerType"]).OptionLabel("Seleccione Tipo de Cliente")
)