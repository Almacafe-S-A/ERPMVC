


@( Html.Kendo().TreeView()
           .Name("TrialBalance")
    //.TemplateId("TreeViewTemplate")
    //  .DataTextField("AccountName")
    //.DataSource(dataSource => dataSource

    //       .Read(read => read
    //           .Action("TrialBalanceRes", "TrialBalance")
    //       ))
    .BindTo((List<ERPMVC.Models.Accounting>)ViewBag.Tree, mappings =>
                      {
                          mappings.For<ERPMVC.Models.Accounting>
                              (binding => binding
                                  .ItemDataBound((item, modelItem) =>
                                  {
                                    
                                      item.Text = "Cuenta: "+ modelItem.AccountCode +"-"+ modelItem.AccountName + "                             "
                                                 +"Debito: "+ "Credito: "+" Saldo: "+modelItem.AccountBalance;
                                      item.Id = modelItem.AccountId.ToString();
                                      item.Expanded = true;
                                    //  item.ImageUrl = "~/Content/web/treeview/" + modelItem.idTipoDoc + ".png";
                                  }).Children(c => c.ChildAccounts));
                      }))

